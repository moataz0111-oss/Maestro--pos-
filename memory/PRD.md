# PRD - نظام إدارة المطاعم والكافيهات (Maestro EGP)

## المشكلة الأصلية
بناء نظام شامل لإدارة المطاعم والكافيهات يدعم:
- إدارة نقاط البيع (POS)
- إدارة الفروع المتعددة
- إدارة الموارد البشرية والرواتب
- إدارة المخزون والمشتريات
- نظام التوصيل وتتبع السائقين
- تقارير وإحصائيات ذكية
- دعم Multi-tenancy (عدة عملاء)

## الجلسة الحالية - 16 يناير 2026 (الإصدار النهائي)

### ✅ ما تم إنجازه بالكامل:

#### 1. نظام المشتريات والموردين
- ✅ صفحة المشتريات `/purchasing` كاملة
- ✅ APIs للموردين وأوامر الشراء والمواد الخام
- ✅ تنبيهات انخفاض المخزون

#### 2. نظام طلبات الفروع
- ✅ صفحة طلبات الفروع `/branch-orders` كاملة
- ✅ APIs لإنشاء وتتبع الطلبات بين الفروع

#### 3. صلاحيات الميزات في SuperAdmin
- ✅ **30 ميزة** قابلة للتفعيل/التعطيل لكل عميل
- ✅ **9 خيارات إعدادات**: المستخدمين، العملاء، الفروع، الفئات، المنتجات، الطابعات، شركات التوصيل، الكول سنتر، الإشعارات

#### 4. تطبيق صلاحيات الإعدادات
- ✅ تم تطبيقها في Settings.js
- ✅ التبويبات المعطلة لا تظهر للعميل
- ✅ تم اختبارها على عميل "البيت العراقي" بنجاح

#### 5. خلفية Dashboard للعملاء
- ✅ زر "الخلفيات" يظهر لجميع المستخدمين
- ✅ نافذة اختيار الخلفيات تعمل بشكل صحيح
- ✅ الخلفية تظهر خلف الأيقونات مع تأثير blur
- ✅ APIs لحفظ واسترجاع الخلفية المختارة

#### 6. فتح الوردية تلقائياً
- ✅ عند تسجيل الدخول، يتم فتح وردية جديدة تلقائياً

#### 7. الشعارات
- ✅ تم إصلاح مسار رفع الشعارات `/api/uploads/logos/`
- ✅ الشعار يظهر في Dashboard للعملاء

#### 8. APIs الحجوزات (جديد)
- ✅ `GET /api/reservations` - قائمة الحجوزات
- ✅ `POST /api/reservations` - إنشاء حجز جديد
- ✅ `PUT /api/reservations/{id}` - تحديث حجز
- ✅ `DELETE /api/reservations/{id}` - حذف حجز
- ✅ `GET /api/reservations/stats` - إحصائيات الحجوزات

#### 9. APIs التقييمات (جديد)
- ✅ `GET /api/reviews` - قائمة التقييمات
- ✅ `POST /api/reviews` - إضافة تقييم جديد
- ✅ `PUT /api/reviews/{id}/respond` - الرد على تقييم
- ✅ `DELETE /api/reviews/{id}` - حذف تقييم
- ✅ `GET /api/reviews/stats` - إحصائيات التقييمات

#### 10. APIs التقارير الذكية (جديد)
- ✅ `GET /api/smart-reports/sales` - تقرير المبيعات
- ✅ `GET /api/smart-reports/products` - المنتجات الأكثر مبيعاً
- ✅ `GET /api/smart-reports/hourly` - تقرير المبيعات بالساعة

### 📊 نتائج الاختبارات
- **Backend:** 20/20 اختبار ناجح (100%)
- **Frontend:** جميع الصفحات تعمل بشكل صحيح

## المهام المتبقية

### 🔴 أولوية قصوى (P0)
- [ ] إعادة هيكلة `/app/backend/server.py` (8000+ سطر - يحتاج تقسيم)

### 🟡 أولوية عالية (P1)
- [ ] تحسين خريطة السائقين الحية (خط السير)
- [ ] إشعارات Push للسائقين (Firebase)
- [ ] إكمال ميزة السحب والإفلات لترتيب أيقونات Dashboard

### 🟢 أولوية متوسطة (P2)
- [ ] التحقق من وظيفة PWA
- [ ] إكمال تكامل أجهزة البصمة (ZKTeco)
- [ ] بناء نظام ولاء العملاء
- [ ] بناء نظام إدارة الوصفات

## البنية التقنية

### Backend
```
/app/backend/
├── server.py              # الملف الرئيسي (8000+ سطر)
├── api/
│   ├── biometric.py
│   ├── login_backgrounds.py
│   └── purchasing.py      # (ملف إضافي - غير مستخدم)
├── uploads/
│   ├── backgrounds/
│   └── logos/
├── tests/
│   └── test_iteration11_*.py
└── requirements.txt
```

### Frontend
```
/app/frontend/src/
├── pages/
│   ├── Dashboard.js       # محدث: خلفيات، شعارات، وردية تلقائية
│   ├── SuperAdmin.js      # محدث: صلاحيات الإعدادات (30 خيار)
│   ├── Settings.js        # محدث: إخفاء التبويبات حسب الصلاحيات
│   ├── Purchasing.js      # المشتريات والموردين
│   ├── BranchOrders.js    # طلبات الفروع
│   ├── Reservations.js    # الحجوزات (مربوط بـ API)
│   ├── Reviews.js         # التقييمات (مربوط بـ API)
│   └── SmartReports.js    # التقارير الذكية (مربوط بـ API)
└── App.js
```

### قاعدة البيانات (MongoDB)
مجموعات جديدة:
- `reservations` - الحجوزات
- `reviews` - التقييمات
- `suppliers` - الموردين
- `purchase_orders` - أوامر الشراء
- `branch_orders` - طلبات الفروع
- `raw_materials` - المواد الخام
- `tenant_settings` - إعدادات العميل
- `dashboard_backgrounds` - خلفيات Dashboard

## بيانات الاختبار

### Super Admin
- Email: `owner@maestroegp.com`
- Password: `owner123`
- Secret Key: `271018`
- URL: `/super-admin`

### Admin النظام الرئيسي
- Email: `admin@maestroegp.com`
- Password: `admin123`
- URL: `/login`

### عميل البيت العراقي
- Email: `ahmed@albait.com`
- Password: `password123`
- صلاحيات محدودة في الإعدادات

---
آخر تحديث: 16 يناير 2026 - 5:00 PM
نسبة الإنجاز: 95%
