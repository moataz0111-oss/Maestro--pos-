[pytest]
# Disable bytecode generation to avoid __pycache__ directories that break
# Emergent's auto-commit mechanism (see conftest.py for full rationale).
addopts = -p no:cacheprovider
cache_dir = /tmp/.pytest_cache
